"use client";

import React from "react";

interface ProviderProps {
  children: React.ReactNode;
}

function _Providers({ children }: ProviderProps) {
  return children;
}

export default _Providers;
